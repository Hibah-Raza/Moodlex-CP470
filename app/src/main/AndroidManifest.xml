<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moodlex">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Moodlex">

        <activity android:name=".SettingsActivity" />
        <activity android:name=".SuggestionsActivity" />
        <activity android:name=".JournalActivity" />
        <activity android:name=".MoodHistoryActivity" />
        <activity android:name=".MoodCheckInActivity" />

        <!-- Main launcher activity — MUST have android:exported when using intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true">   <!-- ✅ Added line -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
